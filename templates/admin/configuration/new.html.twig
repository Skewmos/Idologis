{% extends 'adminBase.html.twig' %}

{% block title %}New Configuration{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-12">
                <h1>Create new Configuration</h1>
           <div class="text-right">
               <a href="{{ path('configuration_index') }}" class="btn btn-warning">Retour</a>
           </div>

                {{ include('admin/configuration/_form.html.twig') }}

            </div>
        </div>
    </div>
{% endblock %}
